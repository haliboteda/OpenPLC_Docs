# 待办

**要做什么已经清楚，只是还没做。** 准入见 [../WHERE-THINGS-LIVE.md](../WHERE-THINGS-LIVE.md)：

> 每一条必须写明它是哪张已关的票产生的。写不出来的，写不进去。

做完且判据过了，**删掉那一行**，不要划掉留着。

| 待办 | 怎么算做完 | 来自哪张票 |
|---|---|---|
| PG9 改输入这件事挪进 `.ioc`，不要留在手写代码里 | CubeMX 重新生成后 PG9 仍是输入，且没有手写补丁 | [问题去哪住](../maps/docs-migration/issues/MIG-09-where-do-defects-and-modules-live.md)（原 `ISS-A3`） |
| 五个通信口做「异常可恢复」：上位机从帧里看出断了（`conn` 掉 0、`miss` 连增），自己计时到恢复 | 产测文档 3.10 的判定栏能填出「误码 / 恢复时间」，人只要动手拔线、不用回来汇报 | 同上（原 `ISS-D1`）。⚠️ 2026-09-16 起**等工装的使用反馈**再动 |
| 上位机面板左边按九个配置项分类分段 | 九类各自成段；**端口本身仍按板子分组**（Bridge / Upper / Lower / 整板），那条已定不要重开 | 同上（原 `ISS-D2`）。⚠️ 同样等反馈 |
| 每个端口逐个打通到真板子上过 | 每个端口在真板子上跑过一轮并记下结果 | 同上（原 PORT-BRINGUP-PLAN.md 整份，该文件已删） |

## 正在做

| 待办 | 怎么算做完 | 来自哪张票 |
|---|---|---|
| **`[BOOT] millis=` 靠电荷泵余电这件事，实测定夺** | **冷启动（断电再上电）时那行还出不出得来**：不出来就证明现状不可依赖，出来就说明余电窗口够。改与不改按实测结果定 | 原 `ISS-B1`。用户 2026-09-16：「你可以自己试一下这个方案，板子电源和线都连着呢」 |

⚠️ **板子当前状态（2026-09-16）**：工装固件 0.10.0 已被 bootloader + `M5_SerialConflict` 覆盖。
**用户说不用装回去** —— 接下来继续做 boot 相关的工作。

⚠️ **顺带修掉一处配置漂移**：`$TOOL/TestCase/config/machine.py` 的 `LOG_PORTS` 写着 `COM12`，
**这台机器上 COM12 已经不存在**，RS232 适配器现在是 `COM5`。已改成 `["COM5", "COM14"]`。
