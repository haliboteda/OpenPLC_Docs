# 词表

**只收「不查就不知道是什么」的词。** 用户 2026-09-16 问「什么叫跨仓镜像、前沿是什么意思」——
那两个词一个是项目自己的，一个是工具带来的，都没人解释过。

## 项目自己的词

| 词 | 是什么 |
|---|---|
| **跨仓镜像** | 同一段代码在两三个仓库里各有一份拷贝。因为 bootloader 和用户 app 是两个独立编译的程序，没有共享构建，谁也 `include` 不到对方。**改一份不改另一份不会编译报错**，只会在运行时表现成别的症状。九条的清单在 [docs/repo/ARCHITECTURE.md](docs/repo/ARCHITECTURE.md) |
| **工装** | 端口测试用的那套东西：板子上的测试固件 + PC 上的 `PortTool.exe` 面板 + `plans/` 里的方案文件。**不是产品固件** |
| **模拟板 / `sim`** | 把同一份 `porttool` 源码编到 PC 上跑，用来在没有板子时验上位机。**不是另写的假板子** |
| **方案文件** | `plans/*.json`。产线测试的步骤序列和判据都在里面。**删了面板就只剩读数** |
| **抄件** | 硬件事实在固件、core、变体头里的拷贝。**抄件不能当唯一出处**，出处只能是原理图 / 网表 / 数据手册 |

## 这套记录方式带来的词

| 词 | 是什么 |
|---|---|
| **图（map）** | 一件事一张图，有一个终点，走到终点就结束。活图清单在 [maps/INDEX.md](maps/INDEX.md) |
| **票（ticket）** | **一个待决的问题**，答完就关 —— **不是一份工作量**，也不是一个功能 |
| **现在能开工的票**（工具里叫 frontier / 前沿） | 没被别的票挡着、也没人正在做的那些。跑 `python tools/list_wayfinder_map_frontier.py` 算出来 |
| **挡着（blocked by）** | 这张票要等另一张答完才问得清楚 |
| **迷雾**（`Not yet specified`） | 知道有这件事，但**还问不清楚**，所以还不能立票。问清楚了就升格成票 |
| **参考层** | `docs/` 底下按问题域分的那八类，收**已经定死、不会因为工作推进而变**的事实和决策 |

⚠️ **对人说话时不要用「前沿」这两个字**，直接说「现在能开工的是这几张」。
用户 2026-09-16 明说看不懂。
