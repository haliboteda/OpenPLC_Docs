# 三十多份文件逐份定去向

Type: task
Opened: 2026-09-16
Status: resolved
Blocked by: MIG-02, MIG-09

## Question

三个仓的全部 `.md` 逐份定去向：搬、合并、还是删。

范围：`open_plc_cube_ide/docs/`（22 份）、`AI-Skills/OpenPLC/docs/`（8 份）、
`IAPTranfer_Tool/` 下的 `TEST-CASES.md`、`checklist.md` 等。

⚠️ 合并和删除都要先问 —— 看起来重复的东西在这个项目里常常是刻意的。

## 怎么算答完

一张表，每份现有文档一行，写明新路径或者「删，理由」。
表覆盖三个仓的全部 `.md`，逐一对得上，没有一份没被提到。

## Answer

2026-09-16 定。表写成 [../../../docs/FILE-DESTINATIONS.md](../../../docs/FILE-DESTINATIONS.md)。

三个仓全部 **43 份** `.md`，一份一行，43 行全部定死，**零待定**。

需要拍板的六件已定（2026-09-16）：

| | 结果 |
|---|---|
| 四份代码旁的 README | **留在原地**，`docs/` 放指针。它们描述的就是所在目录，搬走等于下一个进那个目录的人什么也找不到 |
| INDEX.md、OVERVIEW.md | **删**。搬完目录结构本身就回答「去哪查」，留着是第二份会漂的地图 |
| `WRAP-UP.md` | **逐条对**。被新关票流程接管的删掉并指过去；「不要假设用户要推送」这类新框架里没有位置的必须保留 |
| `STATUS.md` | ⚠️ **2026-09-16 执行时反转了**：整份搬进 `docs/tables/STATUS.md`。原定「劈成两半」做不到 —— 需求和结果是同一行的不同列，不是不同章节。按列劈会产生两份必须逐行对齐的文件，正撞「一个事实只写在一个文件里」，而且会弄坏 P7。「结果列该由脚本生成」继续留在迷雾里等生成器 |
| 两处改名 | `OpenPLC_Bootloader.md` → `BOOTLOADER-PROJECT-LAYOUT.md`；`checklist.md` → `ACCEPTANCE-CHECKLIST.md` |
| 六份「还在动的」 | 由 `已知问题和待立项模块在新框架里住哪` 那张票定完，分流进票 / `work/` / `waiting/` / 迷雾 |

### 验证

`怎么算答完` 要求「表覆盖三个仓的全部 `.md`，逐一对得上，没有一份没被提到」。
拿磁盘上的实际文件和表做集合比对：**43 / 43 全部匹配，对称差为空，待定行 0**。
六行改完之后重跑过一次，结论不变。

⚠️ **给 `P7/P8/P9 和 selfcheck 怎么跟着搬` 那张票的一条**：`STATUS.md` 劈开之后
「结果」那半留在 `AI-Skills`，所以搬完仍有一处产品级内容不在本仓 —— 这会影响
`AI-Skills/OpenPLC 搬空后剩什么` 的答案。

## 引出了什么新的未知

**搬迁的执行顺序本身是个问题，而且有先例。** `CONVENTIONS.md` 记着上一次重组
**弄断 105 处链接，那 105 处全是手工找出来的**。这次要移动 37 份文件，
而 P9 的扫描根现在**还不包含 `OpenPLC_Docs`** —— 也就是说搬迁进行当中，
这个仓是没有链接检查的。**先改 P9 再搬，还是先搬再改**，决定断链能不能被自动抓到。
归 `P7/P8/P9 和 selfcheck 怎么跟着搬` 那张票，不在这张里做。
