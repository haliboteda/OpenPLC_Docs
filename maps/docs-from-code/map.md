# 把设计、需求、测试从代码注释里提出来

## Destination

**文档是最全最准的那一份；代码注释和它同步。**

⚠️ **2026-09-16 用户改过一次终点，改小了**：原来是「把设计从注释里搬出来，注释只说这段代码要达成什么」，
现在是 —— **现有注释的内容可以不动**，用它去**比对并补全文档**，然后让两边同步。

> 用户原话：「现有注释里面的内容可以不动，分模块把这些注释里面的东西跟设计、需求等文档比对，
> **保证文档是最全最准的**，注释都跟文档同步。」

所以这张图**不再是一次搬迁**，是一次**比对补全**：注释是原料，文档是产物，注释留在原地。

## Notes

**用户 2026-09-16 原话：**

> 「我要你把设计、需求、测试从代码注释里全部提取出来，按照这几大块都整理好，精炼去重。
> 注释里的东西，或者说代码里面的东西都是这些内容的体现。**不应该去代码里面找设计。
> 我需要文档和代码分开。**」

**语料是 [CORPUS.md](CORPUS.md)：227 个文件、8,611 行叙述性注释**（排掉 vendored 的
ST HAL / FatFs / micro-ecc / lan8742 / lwipopts；只数长度 ≥ 25 字符的注释行）。
⚠️ **这张表就是「全做完」的锚。** 上一张图的终点写窄了（「三个仓」，实际七个），
判据验得完美却验错了范围 —— 这次先把全集钉死再动手，见
[补记录框架自己的洞](../framework-gaps/map.md)。

**已有的注释规矩**：`~/.claude/rules/comments-state-intent.md` ——
一行注释说明这段代码要达成什么即可，不解释原因、不展开推理。
**这张图是把那条规矩真正执行一遍**，不是新规矩。

**执行到底，不只是规划**（覆盖 wayfinder 默认）。票号前缀 `DFC-`。

**模块顺序（用户 2026-09-16 定）**：先 `boot`，再 `security`，各自把设计文档整理好，再谈别的。

## Decisions so far

- [跨仓镜像的约束现在到底靠什么](issues/DFC-02-do-mirrors-still-need-comments.md)：**9 条里 7 条 P2 真的在查**，第 6 条完全没查、第 9 条只查一半。表已重排并逐位置核实，同步规矩改成「boot 为源 + 改动时问用户」
- [哪些注释算设计/需求/测试，哪些本来就该留在代码里](issues/DFC-01-what-counts-as-design.md)：**三分**（结论+一句话留 / 成段论证搬 / 抄原理图的给链接不抄原文），**单位是段不是文件**

## Not yet specified

- **8,611 行怎么读完。** 人力逐行不现实，但全自动提取会把「为什么这么设计」和
  「这一行在干什么」混为一谈。分批的切法、顺序、谁来判，都还没想清楚
- 提出来之后，参考层现有八类够不够 —— 「需求」已有 STATUS、「测试」已有 TEST-CASES，
  「设计」散在 DECISIONS 和各域，三大块和八类不是一一对应
- **精炼去重的判据**。P8 现在豁免代码块；提取之后同一个事实会同时活在注释和文档里，
  那正是 P8 要抓的，但它现在看不见代码

## Out of scope

- vendored 代码的注释（ST HAL、FatFs、micro-ecc、lan8742、lwipopts）——
  它们不是我们的设计，改了下次升级就没了
- 代码行为本身。这张图只动文档，不动逻辑
- **删注释**。2026-09-16 用户定了现有注释内容不动 —— 这张图只比对和补全文档
- **共享文件**（submodule / 一处为源生成拷贝）。2026-09-16 用户明确放弃：不共享，改成 bootloader 为源、改动时人工确认同步
