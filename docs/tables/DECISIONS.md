# 已决策，不要重新讨论





**这里放已经拍板、有理由、不需要再开会的决定。** 从 `IAP-STATUS.md` 拆出来（2026-08-22），因为它们既不是实测数字也不是待办 —— 它们是"别再问了"。





每条给的是**决定 + 理由 + 什么情况下才该重开**。没有"什么情况下才该重开"的决定不算决定，那叫习惯。





> ⚠️ **什么能进这份：用户拍过板的，或者已经落地并实测过的。**


>


> **AI 的建议不能进** —— 即使看起来很对。建议待在 ../work/ISSUES.md 里，明写"等用户定"。


>


> 这条是建立本文件当天（2026-08-22）就犯过一次才写下的：原来 `IAP-STATUS.md` 的「已决策」只有 **3 条**，本文件一上手写了 6 条，多出的里面有一条是 AI 自己的建议（下面第 4 条，已更正）。**一条写进"不要重开"的建议，比没有记录更难推翻。**





---





## 1 · bootloader 与 app 必须捆绑升级 —— 靠流程兜，不改代码





**metadata 存在 journal 里**（机制见 [JOURNAL.md](../boot/JOURNAL.md)）。新 bootloader 读不懂旧格式 journal → 读不到 metadata → 判定 app 无效 → **停在 bootloader 且不会自愈**，现象和变砖一样。





app 镜像本身一个字节都没变，是 bootloader 找不到那张"出生证明"了。





**决定：不改代码，写进发布说明。** bootloader 升级必须和 app 升级捆绑。





⚠️ **这是唯一一个纯靠流程兜的风险。** 2026-08-16 已写进 ../../RELEASE-NOTES.md（`$BOOT/RELEASE-NOTES.md`） 的 Upgrade rules（含"看着像变砖其实没砖、重传 sketch 即可"的补救步骤）。





**什么情况下重开**：再改 journal 格式或 metadata 位置，**第一件事是同步那一节**。真要做到不捆绑，得让新 bootloader 认得旧格式 —— 那是一个独立立项。





## 2 · 不再追求 boot / app 的 MAC 必须一致





用户 app 可能自定义 MAC。设备定位继续靠 **UDP 广播 + UID 匹配**，**这套兜底不能删**。





保留 bootloader 侧的 UID 派生 MAC，理由变成"每板唯一"（写死的 MAC 会让同网段两台板子直接冲突），而不是"和 app 一致"。





**什么情况下重开**：不重开。这条是产品定位的直接推论 —— 用户有权改自己的 MAC。





## 3 · 发现限流用全设备封顶，不按源 IP





**根因**：Arduino 核心包自带的 `network_discovery.exe`（IDE 一开就常驻）每 30 秒向板子发一次发现请求。板子按**源 IP** 限流（2 秒一次），同一台机器上的 IDE 插件和 IAPTool **共用配额**，谁落在对方 2 秒内谁被拒 —— 概率约 2/30。工具侧表现为 `No response, exiting`，极像板子挂了。





**按源计的粒度才是病根** —— 一台主机上永远可能有第二个程序，调窗口只能让碰撞变稀，消不掉。





放大倍数只有 **1.9 倍**（请求 24 字节 / 回复 46 字节），本就够不上反射放大器（DNS 约 50 倍、NTP 约 500 倍），按源限流买到的安全性远小于代价。





**决定**：`DISCOVERY_MAX_REPLIES_PER_SEC = 50`，全设备封顶，bootloader 和 app 两边同步（**app 之前完全没有限流**）。合法用量约 2 次/秒，离上限差 25 倍，永不误伤。





**超限日志每个窗口只打一行** —— 否则泛洪时 UART 会被自己的日志拖死，日志本身成了比攻击更有效的拒绝服务。





配套还有两处：IAPTool 首次发现重试 3 次（间隔 2.5s 是刻意的，要跨过设备端的节流窗口），以及上传锁（见 `$PROD/docs/repo/ARCHITECTURE.md` 的跨仓镜像表）。





⚠️ **50 不是保证值，是约值。** 固定窗口在突发跨边界时能到近 2 倍，实测过 58/s 和 60/s。要对外承诺一个硬上限就得换令牌桶。





**2026-09-03 拍板：保持现状，不换令牌桶。** 合法用量差 25 倍，从不误伤；换掉要改两份跨仓镜像并重跑 N5。





**什么情况下重开**：要对外承诺限流数字的时候。





## 4 · 交权给 app 时把外设留在冷板子状态





bootloader 在 `server_jump_to_app()` 里主动关掉 RS232 收发器、DeInit UART。它是需求 E4，已实测，理由是"交权时让 app 拿到一块冷板子的状态"。





**什么情况下重开这条**：如果有用户报告它干扰了他们自己的协议解析。





## 5 · FMC 的 39 个脚用可发现性方案，不做运行时拦截





变体头里给它们起名 `FMC_RESERVED_*` 并注释，**不在 `pinMode`/`digitalWrite` 里拦**。





`digitalWrite(PE7, HIGH)` **仍然编得过，这是刻意的**。理由两条：每次 IO 调用加开销；而且一个会拒绝执行的 `digitalWrite` 比一个拽死 SDRAM 的更让人困惑。





⚠️ **需求 E6 的原文是"用户碰不到"，落地时按实际改写成"看得见"。** 需求原文和实现不符时改需求，不要让实现假装满足了它。





**什么情况下重开**：有人真的因为误碰 FMC 脚而烧了板子。





## 6 · app 侧 SDRAM 要封装成库，不给用户链接脚本段





2026-08-17 确认："**要做，而且要封装好，用户直接调封装好的能力。**"





这半句否掉了原来"用户自己在 linker script 段里声明大数组"的方案 —— **`memset` 的义务必须由库承担，不能落到用户头上**。清零 64MB 要 701 ms（91 MB/s 实测），所以库里另给了 `allocUninitialized()`。





三条链接脚本硬约束（`(NOLOAD)`、放在 `_sbss.._ebss` 之外、不能有初值）**仍然是底层实现的硬约束**，只是不再暴露给用户。





**什么情况下重开**：不重开。"清零该由库做，不该写进用户文档让用户记着"是硬判断。





## 7 · 上位机必须零安装分发





2026-09-04 用户定的目标：**"我的目标是在别人机器上运行上位机软件，不用安装任何东西。"**





含义很硬：**拷一个文件过去，双击就能跑。**不装运行时、不装 Python、不解压一堆 DLL、不要管理员权限。





**这条否掉的**：任何要求目标机器预装运行时或组件的形态 —— Electron、Python + pip、以及需要单独部署运行时的 GUI 框架。Go 交叉编译出的单文件 exe 天然满足，`$TOOL/Output/windows/IAPTool.exe` 已经是这样分发的（`compile_tool.sh` 一次出 windows / darwin / linux 三个目录，`go.mod` 里没有任何 GUI 依赖）。





⚠️ **它管不到的一件事：USB-RS232 适配器的 COM 驱动。** 工装必须经串口和板子说话，那个驱动装在操作系统里，不在 exe 里。**这条还没验证** —— 选型时要挑 Windows 自带或 Windows Update 能自动装上的芯片，并且在一台干净机器上实测。





**什么情况下重开**：不重开。这是分发方式的产品约束，不是技术选型。





## 8 · 上位机做成一个 exe 两种模式：双击起本地面板，带参数走 CLI





2026-09-04 用户拍板。承接第 7 条的零安装约束。





- **不带参数双击** → 绑 `127.0.0.1` 起本地 HTTP，自动拉起默认浏览器，面板在网页里


- **带子命令** → 机器可读输出，给产线序列和自动化脚本





**理由三条**：





1. **浏览器是目标机器上唯一一个"已经在那儿、且不需要我们分发或保证"的 UI 运行时** —— Win10/11 自带 Edge。这是它比任何 GUI 框架都直接的地方


2. HTML/CSS/JS 用 Go 的 `embed` 打进同一个 exe，**仍然是单文件、纯 Go**，`compile_tool.sh` 的一键三平台交叉编译不受影响


3. [PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) C.3 那三种用法（手动 / 产线 / 自动化）共用**一份固件**，上位机也该共用**一个二进制** —— 拆成两个程序会立刻出现"两边判据不一致"





**排除掉的**：Wails（需要 WebView2 Runtime，"通常已装"不等于"不用装"）、Fyne（CGO 会砸掉一键三平台交叉编译）、Electron。





⚠️ **三件还没验证的**，都在干净机器上才试得出来：USB-RS232 的 COM 驱动、SmartScreen 对未签名 exe 的首次运行拦截、Windows 防火墙对绑 `127.0.0.1` 的监听会不会弹窗。





**什么情况下重开**：上面三件里有任何一件在干净机器上真的挡住了人，且绕不过去。





## 9 · 回环计数走哪条线，按端口类型自动选





2026-09-07 用户拍板。工装的每个会话都跑「板子发一个数 → 上位机回一个数 → 板子在此基础上累加」的回环，**但这个回环走哪条物理线，取决于端口是什么类型**：





| `loop=` | 哪些端口 | 回环走哪条线 | 这个计数器证明了什么 |


|---|---|---|---|


| `link` | rs485 · can · knx · rs232 · usb · eth | **被测链路本身** | 那条链路双向通。这是真判据 |


| `ctrl` | din · dout · relay · ain · aout · temp | RS232 控制口 | **只证明控制口活着和板子没死循环**。端口判据仍是电平 / 电压 |


| `none` | sd · sdram | 不回环 | 它们不是链路，没有对端 |





**无论走哪条线，状态一律从控制口用 `!<port> … seq= rx= miss=` 上报** —— 上位机只在一个地方读状态。





**理由**：只走控制口的话（当初的简单方案），RS485 / CAN 那几条链路就永远测不到自己那条线。只走被测链路的话，IO 端口压根没有对端。两类端口行为本来就不同，硬统一会让其中一类的计数器变成谎话。





⚠️ **代价**：上位机要同时持有多条串口，面板上的串口设置是一张「角色 → COM 口」映射表，不是一个下拉框。





**什么情况下重开**：不重开。要加端口时只需在 caps 里给它标一个 `loop=`。





## 10 · 八路 DO 的 PWM 用软件 PWM，不用硬件定时器通道





2026-09-07 用户拍板。诉求是「任何一个 DO 都能由工程师选择是否输出 PWM」。





**硬件事实**（核实自 `$HW/LowerDeck_overview.txt:60-67`）：八路 DO 全都在定时器通道上，**但两两共用一个比较单元** —— DO1/DO5 = TIM1_CH1(N)、DO2/DO6 = TIM1_CH2(N)、DO3/DO7 = TIM8_CH3(N)、DO4/DO8 = TIM15_CH1(N)。纯硬件 PWM 只能给 **4 个独立占空比 + 3 个独立频率**。





**决定**：八个 GPIO + 一个定时器中断，ISR 里按各自的 `duty` 翻转。1 kHz × 1% 分辨率 = 100 kHz ISR，M7 @ 400 MHz 下占用 < 1%。





**理由**：硬件方案里「勾了 DO1，DO5 跟着变」这件事在面板上讲不清楚，工程师会当成 bug。这个工装是验证「这一路能不能调功率」，不是标定 PWM 精度 —— 要看边沿质量该上示波器。





⚠️ **未验证**：DO 前级是 VNQ5160K-E 智能高侧开关，不是裸 MOSFET，内部有电荷泵和保护逻辑。**它的 PWM 上限没有数据手册可查**，1 kHz 下极端占空比（<5% / >95%）能不能出得来要示波器实测。所以 `freq` 参数对工程师放开，让他扫频，实测结果反过来补进 [HARDWARE-FACTS.md](../hardware/HARDWARE-FACTS.md)。





**什么情况下重开**：实测发现软件 PWM 的抖动（`printf` 阻塞时几十 µs 量级）真的干扰了判读。那时改成第三条路：默认软件 PWM，DO5–DO8 可切到硬件通道。





## 11 · 上位机面板的文字用中文，大白话





2026-09-07 用户拍板。**这一条只管浏览器面板里给人看的文字。**





| 哪里 | 语言 | 变了吗 |


|---|---|---|


| 浏览器面板的按钮、提示、错误说明 | **中文，大白话** | 新增，以前没有这一类 |


| 固件 printf 与 `OK` / `ERR` 应答 | 英文 | 不变 |


| 两个 exe 的 stdout / stderr、日志文件 | 英文 | 不变 |


| 代码注释 | 英文 | 不变 |





**大白话指的是不堆术语**，跟语种是两件事：写「DI3 没信号 —— 看看 24 V 接上没有」，不写「CH3 assertion failed」。





⚠️ **已知的错位**：面板上的中文提示旁边就是板子来的英文 printf 日志，两种语言混排。这是接受的代价 —— 日志是原始证据，改写它就没法拿去对固件源码了。**`ERR` 行必须原文显示，不许翻译或归纳**（这条本来就是 [PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) A.6 的铁律 1）。





**什么情况下重开**：这套工具要交给不读中文的人用。那时按 `~/.claude/rules/language.md` 的约定补一个语言开关，不是把中文换掉。





## 12 · 面板把每一个可选项都摆出来，子端口拆到最细





2026-09-07 用户拍板：「具有多个子端口的端口，在选择的时候能拆分开尽量可以拆分选择」「可选择越全面越好」。





**拆到最细**意味着：





- 通道逐路可选，且**逐路可设值** —— din 8 路、dout 8 路、relay 6 路、ain 2 路、aout 2 路、temp 2 路


- caps 报的每一个 `params=` 都渲染成控件，没有「高级选项」折叠


- 底部常驻一个原始命令输入框，能敲任意 `pt.*` 行 —— 面板做不到的事不能变成做不到





**两处连带改动**：





1. **relay 要改成逐路设电平。** 现在 `on=` 对所有选中通道统一驱动（[PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) B.2 写了「同一帧内各路必定同值」），要改成 `on=1:1,2:0,3:1`，和 dout 的 `duty=1:20,2:50` 共用同一个 `ch:值` 解析器。


2. **RS232 端子做成会话，不是只当日志通道。** 见第 13 条。





**什么情况下重开**：不重开。这是「工装不替工程师做减法」的立场 —— 判定和序列全在上位机（[PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) C.3），面板藏起来的选项等于固件白做了。





## 13 · RS232 端子用回环会话来测



2026-09-07 用户拍板：「rs232 可以选择自动发自动收，别的情况都是作为其他端口 log 输出的渠道」。





**这一条更正了本文件之前没写、但 [PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) B.3.3 写着的一个结论**：「工装占用的就是这条通道，所以会话模式测不了 RS232 端子本身」—— **这句话是错的**。





**为什么错**：命令字节和应答字节本来就物理穿过 C05/C06 端子、MAX3221 ±12 V 收发器、PC10/PC11。所以给 rs232 开一个 `loop=link` 会话（板子周期发帧，上位机回 `pt.echo rs232 <n>`，板子累加），走的就是被测链路本身 —— 它和 rs485 那种「链路回环」是同一个机制，只是这条链路碰巧也承载命令。








**面板上 rs232 这张卡有两个模式**：自动发自动收（周期会话，可设 `period` 和发什么 `text=`），以及手动 —— 敲任意字节、看回显的 ASCII + hex 两列。





**什么情况下重开**：不重开。





## 14 · `PORTTOOL_ENABLE` 手工开关，不给它单独的构建配置





2026-09-07 用户拍板。工装接进 `main.c` 的时候有两条路，选了改动小的那条。





**做法**：`Core/Src/main.c` 的 `USER CODE BEGIN SysInit` 块里加一段 `#if PORTTOOL_ENABLE`，位置和写法与旁边那排 `#if KNX_TEST_ENABLE` 一致，并进同一条互斥 `#error`（这些入口没有一个会返回，同时只能开一个）。**符号本身不进 `.cproject`** —— 要出工装镜像，在 CubeIDE 的工程属性里手工加 `PORTTOOL_ENABLE=1`。





**排除掉的**：复制一份 `Debug` 成 `PortTool` 构建配置。那样切下拉框就能出两种镜像、业务镜像零风险，但 `.cproject` 变大，CubeMX 重新生成时多一处要盯。





⚠️ ~~**接受的代价，就是这条路唯一的失败模式**：忘了把符号改回去，就会发出一个开不了机的 bootloader。~~





**这个失败模式 2026-09-08 消掉了，决定本身不变。**





**发现**：CubeIDE 的 headless 构建能在命令行上传符号 ——





```


stm32cubeidec.exe … -application org.eclipse.cdt.managedbuilder.core.headlessbuild \


    -data <workspace> -D PORTTOOL_ENABLE=1 -cleanBuild open_plc_cube_ide/Debug


```





**而且它不写进 `.cproject`**（当天核实：这样构建之后 `git status` 报该文件无改动，md5 与构建前一致）。所以符号只活在用它的那一次构建里，**工作区从来不进入那个危险状态**，也就没有「忘了改回去」这件事。





**做法**：`$TOOL/TestCase/tools/build_image.py`。不带参数编 bootloader，`--porttool` 编工装，`--both` 两个都编且**bootloader 放最后** —— 因为 `Debug/` 里留下的那个才是随手一烧会烧进去的东西。脚本还交叉验证了两件事：编工装时构建日志里**必须**有那条 `#warning`（没有就说明符号没到编译器，编出来的其实是 bootloader），编 bootloader 时**必须没有**。





**保留的**：`#warning` 本身，它现在是脚本的判据而不只是给人看的提醒。





### 2026-09-08 补：链接脚本也按同一个原则分开了





用户当天要求「工装用一份 `ld`，正常用一份」。**做法沿用这一条的原则，没有加构建配置**：





- `.cproject` 里链接脚本那个选项的值改成 `${workspace_loc:/${ProjName}/${PLC_LD_SCRIPT}}`（**一行改动**）


- 变量的工程默认值 = bootloader 那份，写在 `.settings/org.eclipse.cdt.core.prefs`


- 工装构建靠 headless 的 `-E PLC_LD_SCRIPT=STM32H743IIKX_FLASH_PORTTOOL.ld` 临时换掉





**为什么还是不加第三个构建配置**：不需要了。`-E` 和 `-D` 一样只活在那一次构建里，所以「一行改动 + 一个默认值」就拿到了原本要复制一整份配置才能拿到的东西，而 `.cproject` 只多一处要盯，不是多一整个配置。





**新增的失败模式，以及它怎么被兜住**：拿错脚本是**静默**的 —— 工装拿到 bootloader 那份只是装不下，而 **bootloader 拿到工装那份会链接到 app 区上面去，烧下去就把 app 和 owner 记录一起盖掉**。所以 `build_image.py` 现在从构建日志里核对链接器实际拿到的是哪一份，不对就当场判失败。重新生成后要查的两处写在 [CUBEMX-RULES.md](../build/CUBEMX-RULES.md)。





**什么情况下重开**：不重开。原来那条「发版检查单要确认日志里没有这条 warning」现在由脚本自动做了。





## 15 · 面板的推送用 SSE，不用 WebSocket





2026-09-07 实现时定。承接第 7、8 条的零安装约束。





**决定**：板子来的行用 **Server-Sent Events**（`text/event-stream`）推到页面，命令走普通 `POST`。





**理由**：Go 标准库没有 WebSocket，上它就要往 `go.mod` 里加第三方库（`gorilla/websocket` 之类）。而第 7 条的约束是零安装、单文件、纯 Go，第 8 条明确写着「HTML/CSS/JS 用 `embed` 打进同一个 exe，仍然是单文件、纯 Go」。**SSE 是标准库，一行依赖都不加。**





方向也正好对：这条链上**需要推的只有一个方向** —— 板子 → 页面（采样帧、日志、应答）。页面 → 板子是零星的命令，`POST` 够用，而且比在 WebSocket 上自己发明一套请求/应答配对更简单。浏览器的 `EventSource` 还自带断线重连。





**代价**：SSE 是单向的，所以命令和推送走两条 HTTP 请求，不是一条连接。这在 `127.0.0.1` 上不构成问题。





**什么情况下重开**：面板需要双向低延迟（比如把某个端口的原始字节流做成交互终端）。那时再评估值不值得加一个依赖。





## 16 · 回环多一种类型 `loop=self`，只给 RS232





2026-09-07 实现 rs232 会话时发现原来的三种分不开一种真实情况。





**问题**：第 9 条把回环分成 `link`（走被测链路，计数器是判据）、`ctrl`（走 RS232 控制口，计数器只说明控制口活着）、`none`（不是链路）。**RS232 端子两边都不是** —— 它的回环机制上走控制口，但那条控制口**就是被测的那个端子**，字节双向穿过 C05/C06、MAX3221 和 PC10/PC11。





**如果硬塞进 `ctrl`**，面板会照 `ctrl` 的规矩在它下面写「这个数只说明控制口活着，这个端口好不好看上面的读数」—— 而 rs232 上面**没有别的读数**。那句话就是在屏幕上说谎，而且骗掉的正是它唯一的判据。





**决定**：加第四种 `self`，含义是「被测链路和控制口是同一条线，所以控制口的往返确实证明了这条链路」。`pt.echo` 对它是**接受**的（和 `ctrl` 一样），但面板按「计数器就是判据」来显示（和 `link` 一样）。





**只有 rs232 会用到它。**这不是遗憾 —— 一个只有一个成员的类别，比一个把两种含义混在一起的类别诚实。





**什么情况下重开**：不重开。





## 17 · 一块硬件在 caps 里只占一行



2026-09-07 实现时定。一块硬件在 `pt.caps` 里出现两行，面板就会为它画出两张卡，而且分不清哪张是哪张。所以一个 `port=` 只允许出现一次：一次性的检查以 `runs=` 挂在同名端口那一行上（`sd` / `sdram` / `eth`），不另起一行。






| | 谁 | 效果 |


|---|---|---|

















**什么情况下重开**：不重开。用例 H4 检查「没有两行共用同一个 `port=`」。



## 18 · 对端把板子发来的那一行原样送回去，不加一





2026-09-07 实现多 COM 绑定时定。`loop=link` 的端口需要一个第二串口适配器接在被测端子上当对端，面板在那条线上做应答。





**决定**：对端**原样重复**收到的那一行，一个字节都不改。





**为什么不是「加一再送回」**（需求 R4 的原话是「返回一个数据，端口收到后再在此基础上累加计数」）：**累加是板子干的**。固件的 `PortTool_EchoTick()` 拿收回来的数和自己刚发的数比对，相等才认为这一拍闭合，然后 `seq = rx + 1`。所以对端一改内容，板子就判成「过期回复」，**看起来和链路死了一模一样**。





「在此基础上累加」这句话描述的是**板子那一侧**的行为，不是对端的行为。





**连带两条拒绝**：





- **绑给 `loop=ctrl` 的端口 → 拒绝。**那种回环走控制口，第二个串口什么也不做，而人会以为绑了就该闭合，转头去查一段不存在的接线故障。


- **控制口本身不能兼任对端 → 拒绝。**同一个串口两个角色，收发会互相吃掉。





**什么情况下重开**：不重开。这条是由固件那侧的比对逻辑决定的，不是偏好。





## 19 · 产线工装板由硬件工程师做，软件只出接口清单





2026-09-07 用户拍板。硬件工程师的产线测试指南（`OPLC-MFG-TEST-001`）里反复出现「PC external companion board」—— AI/AO 校准、DI 激励、高边与继电器的电压电流判定全部指向它，而这块板目前不存在。





**决定**：**这块板归硬件工程师做。** 软件侧交付一份 [FIXTURE-INTERFACE.md](../outbound/FIXTURE-INTERFACE.md)：每个通道给什么激励、读什么量、精度要求、经什么总线报给上位机、上电默认态。





**为什么不是软件顺手用现成仪器凑**：程控电源 + 万用表 + 信号源这条路每台机器都要配仪器、接线多、产线节拍差。工装板是一次性投入，且做完以后上位机侧只剩一个驱动。





**什么情况下重开**：硬件侧明确做不了这块板。那时退回「现成仪器 + SCPI/Modbus 驱动」。





## 20 · 产线的 SN / 标签 / 过站 / 装箱入库不做，只留接口





2026-09-07 用户拍板。产线测试指南 2.5 把这些归给「系统管理软件」，2.6 还要求 SN 由受控服务分配、禁止离线拷号。





**决定**：测试软件只负责**测 + 判 + 出一份结构化报告**。两个口：**SN 从外部读入**，**逐项原始值往外部写出**。SN 池、防重、标签打印、12 工站过站记录、四板绑定、箱号、入库 —— **一条都不做**。





**为什么**：那是 MES / ERP，工作量比测试软件本身大，而且「受控服务分配、写后回读、查重」这类要求本地实现满足不了，做了也不合规。





**什么情况下重开**：不重开。真要做，那是一个独立立项，不是给 PortTool 加功能。





## 21 · 产线烧写走 SWD 整片重烧，不走 IAP





2026-09-07 用户拍板。产线要求工站 6 用测试固件、工站 10 全项通过后烧用户固件并回读版本确认。而工装镜像是 `PORTTOOL_ENABLE=1` 编出来的独立镜像，**顶掉 bootloader**（第 14 条）。





**决定**：**不改镜像形态。** 工站 10 用 JLINK 重烧整片（bootloader + app），上位机侧要的是一层 JLINK CLI 包装加版本回读，**不是**让工装去走签名和 takeown 那一套。





**为什么不把工装做成 app 走 IAP**：PortTool 的受众是硬件工程师和产线，他的工具里刻意不该有固件签名和 takeown（见 `$TOOL/CLAUDE.md`）。而且产线测试指南 2.4 已经把 SWD/JTAG 编程器排在工站 1 和工站 6，多用一次不增加设备。





**什么情况下重开**：产线最终不配 JLINK。那时才需要重新考虑工装与 bootloader 共存。





## 22 · 判定一律在上位机，固件只吐原始值





2026-09-07 用户拍板。porttool 本来就明写「固件不判定」（`../../TestCase/porttool/porttool.h`：*"Nothing here judges anything"*），这一条把它定成整个产线侧的规矩。





**决定**：**上位机判为主。** 限值全部进 PC 侧的标准文件，固件只报测出来的数。





**为什么**：产线限值现在大部分是 TBD，试产阶段会反复改。**改一个限值不该重烧固件** —— 这正是 [PORTTOOL-FLOW.md](../production/PORTTOOL-FLOW.md) C.3 已经写下的那条（「判定和序列全在上位机，固件只报原始采样」）。





**这条约束的是产线路径**（`pt.run` 的每个目标）。








⚠️ **也不管 `$TOOL/TestCase/onboard/` 的 Arduino sketch。** 那个 sketch 吐 `RESULT <name> PASS|FAIL` 给 `run_sdram.py` 汇总，但它测的是**给用户 sketch 用的 SDRAM 库**（需求 E5 / 用例 SD1），和产线端口测试无关。**2026-09-07 本条初稿曾把它误当成「唯一违反本条的地方」，核实后撤回。**





⚠️ **这条不改「谁去测」，只改「谁下结论」。** 内部自测（SDRAM 走线、SD 读写）仍然只有板子做得了，中间数据量也不适合从串口流出去 —— 板子照样跑完整个测试，只是报出来的是数不是结论。





**什么情况下重开**：某一项的原始数据量大到串口报不出来，且压缩后仍然报不出来。那一项才单独例外，并且要在这里记一条。





## 23 · 五个通信口的判据逐口定，不按文档强行统一





2026-09-07 用户拍板。产线测试指南对以太网 / USB / CAN / RS485 / KNX **用了同一句话**：「上传/下载文件到固定设备，读速率并判定」。





**决定**：**逐口定各自合理的判据。**





| 口 | 判据 |


|---|---|


| 以太网 / USB | 真吞吐量（这两个口速率有意义，而且现在是零，要从无到有） |


| CAN / RS485 | 固定帧数无丢包 + 错误计数为 0 |


| KNX | 电报收发 + 位时序（保持现有测法） |





**为什么 KNX 不能照文档做**：KNX-TP1 是 **9600 bps** 的电报总线。要「传文件」得自己造一层分包重组，一个文件要很久，产线节拍撑不住，**而且测不出 KNX 真正会坏的东西** —— 会坏的是位时序和光耦驱动，那正是 `../../TestCase/KNX/knx_test.c` 现在做的边沿直方图在测的。





**连带一件**：**要请硬件工程师把测试指南那五行改成逐口判据。** 文档不改的话，产线审核时会拿「未按规程执行」说事。





**什么情况下重开**：不重开。这条是由总线物理特性决定的，不是偏好。





## 24 · 产线测试做成「方案文件 + 步骤序列」，判据是步骤参数





2026-09-07 用户拍板，依据是他给的另一个项目的上位机测试软件截图（`AiLinkFactoryAutoV0.01`，三栏：方案文件树 / 步骤表带复选框 / 属性网格）。读解与格式在 [PRODUCTION-FRAMEWORK.md](../production/PRODUCTION-FRAMEWORK.md)。





**决定**：





- **一份「方案文件」装一整套测试**（步骤序列 + 每步参数），换产品换文件，不改程序


- **判据不是独立一层，是步骤自己的参数**。所以没有「限值表」这种东西 —— 限值住在需要它的那一步里


- **每个步骤都有六个框架级通用字段**：`ExecuteCondition`、`RetryCount`、`RetryInterval`、`SleepTimeBefore`、`SleepTimeAfter`、`Timeout`


- **跳步 = 取消勾选**，不做权限体系


- **面板里加一个「方案」页做属性网格**，参数控件照现有 caps 驱动那套逻辑生成





**为什么判据不单独成层**：原来的设计打算把限值抽成一张独立的表，由执行器查表判定。参考架构给出了更简单的答案 —— 一个步骤知道自己要判什么，把限值放进它的参数里，就不需要「步骤 ↔ 限值」这一层对应关系，也不会出现「表里有一条没有步骤用它」这种漂移。





**为什么那六个字段必须在第一版**：它们是「框架」和「一堆脚本」的分界线。`ExecuteCondition` 少了，序列只能一路跑到底，表达不了「前面失败就跳过后面」；`RetryCount` / `RetryInterval` 是治具探针接触不良造成**假失败**的直接对策（产线测试指南风险 R10）。





**什么情况下重开**：不重开。





## 25 · 步骤类型只做三个通用的，端口写在参数里





2026-09-07 用户拍板。参考架构里每个功能一个手写步骤类型（`UartCheck`、`SoundRecording`…），我们不照抄。





**决定**：三个通用类型 —— `PtSession`（起会话、采 N 帧、逐字段判限值）、`PtRun`（一次性动作）、`PtRaw`（敲任意命令）。**端口名、通道、参数、限值全写在步骤参数里。**





**为什么**：我们比参考架构多一样东西 —— `pt.caps` 自描述。固件加一个端口，方案文件加一步就能测，**不用改 exe**。做成「一个端口一个步骤类型」反而要写一层从 caps 派生类型的代码，而且 caps 现在只报参数名不报区间，那一步得先做完。





**代价**：属性网格里 `PtSession` 这一类的参数是通用的（`port` / `ch` / 判据列表），不像专有类型那样只显示该端口的字段。可读性差一点，换来的是加端口不动程序。





**什么情况下重开**：caps 补完参数约束（区间 / 单位 / 默认值）之后，如果属性网格的可读性真的成了产线的抱怨，再考虑从 caps 派生专有类型。那时是纯增量，不用推翻这三个通用类型。





## 26 · 面板按方案文件启动端口，也按方案文件判定





2026-09-08 定，起因是一次浏览器自动化测试。





**决定**：面板上「开始测试」这一下，用的参数**来自方案文件那一步**，不是端口的出厂默认值；判据也来自同一步，由 `internal/ptcheck` 求值。方案文件里的参数会**预填进那张卡的输入框**，所以屏幕上看到的就是要发出去的命令 —— 人改了输入框，就是明知故改。





**为什么**：判据是**按某组参数写的**。can 的判据写给 `mode=extloop`，用默认的 `mode=normal` 起会话再拿那组判据去判，好板子会被判失败。反过来也一样：继电器那一步要求 `on=1:1…6:1`，判据查 `ch1` 和 `ch6`，而板子的默认是 `on=1:0` —— 用默认值起会话，板子什么都没驱动却答得很高兴，好板子照样被判失败。**这两个坑都真踩过**，后者是 2026-09-08 浏览器测试抓到的：逐通道参数（`on=` / `mv=` / `duty=`）当时没预填。





**代价**：面板必须知道方案文件的名字（`internal/ptpanel/judge.go` 里一个常量，可换）。没有方案步骤的端口就没有判据 —— 那时面板报「未测」并说明原因，不报通过。





**关掉的那一步也算数**：`enabled: false` 只是说「产线不跑这一步」，不是说「这一步没有判据」。老化就是这个情形 —— 30 秒的工位不该跑老化，但有人在台面上手工起它，需要和整机架跑出来一样的结论。所以 `station6-poweron.json` 里那一步是关着的，判据照样是面板的判据。





## 27 · 端口的结论有四种，「没人能从这儿判」和「还没人判」要分开





2026-09-08 定。





**决定**：⬜ 未测 / ⏳ 测试中 / ✅ 通过 / ❌ 失败，再加一个 **👁 人工判** —— 给上位机读不到判据的项，例如只有人眼能确认的状态灯。





**为什么**：这两类端口原来显示「未测」，读起来像「谁忘了测」，于是人会去找一个不存在的按钮。而且 JS 里三态必须真的是三态：判据不存在时返回布尔，调用方只判 `=== false`，`undefined` 不是 `false`，**没有判据的目标就被算成通过了** —— 这个 bug 2026-09-08 真实存在过。





**代价**：这两个端口的过没过只能靠人。它们本来也只能靠人。





## 28 · 网口做成会话，`eth.link` 挂在同一行上；工装镜像因此要链 lwIP





2026-09-08 定。





**决定**：`eth` 从 `kind=run` 改成 `kind=session`，一次性的链路探测 `eth.link` 以 `runs=` 留在同一行 —— 和第 17 条「一块硬件一行」是同一条规矩。会话本身是一个 TCP server，模式 `echo` / `sink` / `source`。





**为什么**：产线要在网口上连续传大文件并看着进度走，那是「开着的状态」，不是「跑完回一行数字」。`pt.run eth.tcp secs=10` 这种一次性形状做不到：跑的那十秒面板是黑的，也没法让人一直往里灌文件。另起一个 `ethtcp` 端口能绕开形状问题，但一个 RJ45 会在面板上占两张卡，正是第 17 条要避免的。





**代价，比协议改动大得多**：





- 协议只是加字段 —— 固件 caps 一处形状，`internal/ptproto/caps.go` 的 `KindSession` 分支补 `runs=` 解析。老固件配新面板照跑。


- **真正的代价是工装镜像要把 lwIP 链回来。** `PortTool_Run()` 的 `noreturn` 原本就是为了让链接器丢掉 lwIP / USB / mbedTLS（见 `TestCase/porttool/porttool.h` 的头注释）。现在 `PortTool_Run()` 自己调 `MX_LWIP_Init()`、自己在 superloop 里调 `MX_LWIP_Process()`，这一整套就回来了。**尺寸上没问题**：工装镜像走 ST-Link 整片烧，占整片 2048K，120K 那条门禁只属于 bootloader（第 14 条的补记）。


- `ETH/eth_test.h` 那段「本文件故意不带 lwIP / DMA / RMII」的话**对它自己仍然成立**，但对镜像不再成立了。它继续只用 MDIO，所以 `eth.link` 在没插网线、没有 DHCP 的工位上照样能给出 PHY 身份和链路状态 —— 这是保留它的理由。





**没有 DHCP 的工位怎么办**：会话的 `ip=` 参数收静态地址，默认 `dhcp`。产线工位不保证有 DHCP 服务器，而拿不到地址的板子会让整个网口测试变成「测不了」而不是「不通过」。





## 29 · 模拟板是「真固件编到 PC 上」，不是另写一个假板子





2026-09-08 定。用户当时手上没板子，要先把上位机和固件的联调做掉。





**决定**：模拟板 = `TestCase/porttool/` 那些 `.c` **原样**编成 PC 可执行程序，只换掉底下的外设 stub 和最外层的 main（`$TOOL/TestCase/host/porttool_caps/harness/sim_main.c`，`python build.py --sim` 编）。它在 stdin/stdout 上说端口工装协议，上位机用保留端口名 **`sim`** 连它。





**为什么不用 Go 另写一个**：`porttool_plan` 里那个手写假板子就是反面教材 —— 今天加了一个 `sd.integrity` 目标、把 `eth` 改成会话，它**三处**跟不上（不认新目标、caps 里还是 `kind=run`、行数对不上），全靠人手工同步。而 `pt.caps` 自描述这一整套设计（第 17 / 25 / 28 条）的前提就是「固件是唯一事实源」，再养一个会漂的副本等于把那个前提拆掉。真固件编过来，固件改了它直接编不过，漂不了。





**因此它证明什么、不证明什么**：





- **证明**：命令解析、`pt.caps` 的形状、会话启停、echo 计数、帧格式、版本号、以及上位机整条链路（`ptboard` → `ptseq` → `ptcheck` → 报告 → 面板）。**H5 浏览器测试因此不再需要真板子**（`run.py --port sim`）。


- **不证明**：任何硬件行为。读数全是 stub 造的一块理想板子，每根对端线都当接好的。UART 中断收发、`rx_errors`、真 ADC、真 PHY 都不在里面。





**防「拿模拟结果当真板结果」**，四道：开机横幅写 `NOT A BOARD`；UID 是 `00FACADE00FACADE00FACADE`；面板端口标签写明「不是真板子，读数全是假的」；**只有本机编过模拟板时才出现在端口列表里** —— 产线 PC 只拿到 exe，那个选项不存在。





**`sim.` 前缀的后门命令**（`sim.din` / `sim.vdda` / `sim.ain` / `sim.temp` / `sim.link` / `sim.walk`）用来造故障，看面板怎么显示失败。它们由 `sim_main.c` 自己拦下，**永远不进固件的 `dispatch()`** —— 前缀就是承诺：真板上一条都没有，固件的命令表一个字没为它们改过。





**当天就有回报**：它立刻暴露了两个真板上被掩盖的洞 —— `station6-poweron.json` 里 sd 有三个 run 目标却只给了两个判据（真板上「没插卡」把它盖住了），以及 eth 会话没有判据。两个都补了。





## 30 · 限值靠「换一份方案文件」调，不给可编辑的输入框





2026-09-08 定，起因是用户问「0.1 % 可以跳么，能不能默认 0.1 也可以手动调整」。





**决定**：面板的判据来源是**一整份具名方案文件**，在头部一个下拉里选，旁边显示那份文件的 `limit_version`。**屏幕上没有任何一个限值是可以直接编辑的。** 现在有两份：`station6-poweron.json`（严）和 `station6-poweron-relaxed.json`（宽）。换判据会**清掉页面上所有已有结论**，并往日志里写一行。





**为什么不给输入框**：产线指南自己写着**「禁止放宽限值放行」**和「跳步要权限 + 电子签名」（PRODUCTION-TEST-GAP.md E 组，两条现在都没实现）。屏幕上改一个数字**不留任何痕迹** —— 那正是这两条要防的事。而换一份具名方案，**方案名和 `limit_version` 会进每一份报告**，所以「这块板是按宽限值放的行」这件事永远查得出来，不需要先做电子签名就已经有追溯。





**宽版放宽了什么，没放宽什么**：只放宽**真的是测量值**的读数 —— `vdda`（2400–2600 → 2350–2650）、两路板载温度、`soak` 的 `tmax`、`ain` 的原始计数下界（700 → 660，悬空带是 200–650）。**`aout` 的 mV 没放宽**：那是回读的 DAC 设定值，不是测量值，板上没有任何通道能读回自己的模拟输出，放宽它没有物理意义。





⚠️ **这条管的是限值，不是参数。** 参数（周期、字节数、波特率）可以在面板上改并存回方案文件 —— 见[第 34 条](#34--面板改了参数就存回方案文件限值仍然不可编辑)，那里写了两者的界线。





**⚠️ 这份决定没有回答 0.1 % 那个问题。** 在 JP1–JP9 档位定下来之前，端子到引脚的换算系数是未知的，**两份文件都只能判「不是悬空」，写不出任何百分比限值** —— 不是难写，是没有分母。档位定了以后，百分比落在 `analog-in` / `analog-out` 两步上，那时这两份文件才真正分道扬镳。Q2 那个问题（0.1 % 是不是硬指标、要不要逐板校准）仍然等硬件工程师回话。





## 31 · `CDC_Receive_FS` 的那个 `#if` 是防火墙，不是开关





2026-09-08 定，做 USB 会话时发现的。





**决定**：`USB_DEVICE/App/usbd_cdc_if.c` 的 `CDC_Receive_FS`（在 CubeMX 的 `USER CODE BEGIN 6` 块里）按 `PORTTOOL_ENABLE` 分流：工装镜像调 `PortUsb_Received()`，bootloader 调 `IAP_data_recv()`。include 区同样分流，工装镜像不 include `IAP_server.h`。





**为什么不能只留 `IAP_data_recv` 让工装忽略它**：`CDC_Receive_FS` 引用 `IAP_data_recv`，而 `IAP_data_recv` 拖着整个 IAP 服务器、验签、mbedTLS。在 USB 会话出现之前这不成问题 —— 工装从不调 `MX_USB_DEVICE_Init()`，`--gc-sections` 把整个 USB 栈连同这个文件一起丢了。**USB 会话让这个文件重新可达，那条引用也就跟着活了。** 这个 `#if` 是唯一挡住它的东西。





**核过，不是推理**：工装镜像 `nm` 出来有 `MX_USB_DEVICE_Init` 和 `PortUsb_Received`，而 `IAP_data_recv` / `IAP_task` / `IAP_servers_start` / mbedTLS **一个都没有**。镜像只从 153,956 涨到 159,224 字节（+5,268），也印证了这一点 —— IAP 那套进来的话会多几十 KB。





**什么情况下会被破坏**：有人觉得这个 `#if` 多余把它删掉（"工装又不会收到 IAP 命令"）。那时镜像会突然大几十 KB，而且工装镜像里躺着一个能擦 app 区的服务器。**改这一块之前先 `nm` 一遍。**





## 32 · KNX 测到报文层，极性由校验字节自己回答





2026-09-09 定。起因是用户要的东西很具体：**「发送符合 KNX 的报文，不是单纯的字节，并且收到什么打印什么」**，而且**「后面硬件改好了，我就不想改代码」**。





**决定**：`knx` 会话加第三种模式 `mode=frames`，组一条真的 L_Data GroupValueWrite 整帧发出、收回、算校验字节，收发各自一行事件帧。目标组地址 `ga=`、源地址 `src=`、值 `val=` 都从上位机设，面板上是输入框。





**收到的每一帧都同时给两种读法**，`crc=` 说哪一种通过了校验字节：





| `crc=` | 含义 |


|---|---|


| `raw` | 收到的字节原样就是合法报文 |


| `inv` | 要按位取反才对 —— 极性反了 |


| `ack` | 单字节应答，没有校验字节可判 |


| `bad` | 两种读法都不对 |





**为什么是「两种都报 + 校验字节裁决」，不是「加一个 invert 开关」**：用户明确说不想以后再改代码。一个开关意味着硬件修好那天要回来改它的默认值。而 `crc_raw` 对 `crc_inv` 两个计数是**运行时**的答案 —— 硬件改了，数字自己换边，代码一行不动。这也顺便回答了用户最初的问题「看看哪种符合 knx 协议」：不用人看，校验字节裁决。





**实测（真板子 + 用户的真实 KNX 装置）**：90 秒 47 发 / 47 收 / **47 个 `L_Ack ACK`**，`crc_raw=47 crc_inv=0 crc_bad=0`，`bad=0 dropped=0`。**用户在 ETS5 总线监视器里看到了这 47 条。** 那些 ACK 只可能来自装置里的真实设备 —— 固件没有自动应答逻辑 —— 所以这一条同时证明了**真实 KNX 设备认得板子发的报文**。





结论：极性理解在**位/字符层**是反的，但 `knx_slot_level()` 又取反一次，两次抵消，**字节层和报文层都是正的**。所以极性这件事不影响 KNX 可用性，只影响 `w_avg`/`d_avg` 的角色（`d_avg` 才是那个该接近 35 µs 的数）。





**默认组地址 `31/7/255`，源地址 `15.15.250`**。GroupValueWrite 会真的驱动订阅那个地址的执行器，所以默认必须是真实工程最不可能分配的那一个：主组是从 0 往上按功能发的，地址空间的顶端就是那个安静的角落 —— 和当初选 `15.15.250` 作源地址同一个道理。`ga=none` 完全停发。





**⚠️ `31/7/255` 打包出来正好是 `0xFFFF`。** 第一版拿这个值当「没给地址」的哨兵，于是 `ga=31/7/255` 被当成了 `none`，而当时那条「边界值接受了」的测试是假通过。**现在有独立的 `knx_ga_set` 标志，65536 个位模式全是合法地址。**





**`KNX_TX_ENABLE` 的含义也跟着收窄了**：它现在只管 `KNX_Test_Run()`（老的 bring-up 入口）要不要按固定节拍自动发，**不再决定板子有没有发帧的能力**。组帧 / 发帧 / 总线仲裁三个函数移出了那个守卫 —— 它们只用守卫外的原语，所以这一步很干净。这样「打开报文发送」就不等于「打开一个写死地址的自动发送」，后者仍然关着。





**`bus=` 的判据同时改成只看 PH12**（见 [HARDWARE-FACTS.md](../hardware/HARDWARE-FACTS.md) 的拔线实测）：那是三个引脚里唯一跟着总线走的。`ok=`（PD7，恒 0，连总线断电都不动）和 `idle=`（PA10，空闲本来就是高）照旧报出来，但不参与判定 —— 拿它们判，好板子会被判成 `bus=odd`。





## 33 · 工装镜像不为 flash 里的任何东西让路





用户 2026-09-09 的原话：**「工装代码不用考虑 flash 里面有什么，应该做什么直接处理。」**





**决定**：工装镜像是**独立固件**。它整片烧、不走 IAP，所以在它眼里 flash 里没有 bootloader、没有 app、没有 owner 记录区。要测什么就直接测，不为那三样避让地址、不为它们保留代码、不为「万一将来要共存」牺牲形态。





**这条已经体现在**：链接脚本 `STM32H743IIKX_FLASH_PORTTOOL.ld` 拿整片 2048K；没有尺寸门禁（122,880 那条只属于 bootloader）；`CDC_Receive_FS` 的分流把整个 IAP 服务器和 mbedTLS 挡在镜像外（[第 31 条](#31--cdc_receive_fs-的那个-if-是防火墙不是开关)）。





**这条允许的事**（写下来是因为之前在这上面犹豫过）：为了让某个端口测得通，可以直接改 `TestCase/` 下的任何东西、直接把能力从老入口搬出来、直接加新的会话和模式。不需要先论证「bootloader 会不会受影响」—— 它不在这张镜像里。





**这条不允许的事**：把这个原则读成「工装镜像里可以留一个能擦 app 区的服务器」。第 31 条那个 `#if` 不是「为 flash 让路」，是**不让工装带上它不需要的破坏能力**。两件事方向相反。





### 工装也不考虑任何安全机制





用户 2026-09-10 的原话：**「工装不要考虑任何安全，防重放一类的事。」**





**所以**：签名、证书链、挑战认证、`iap_auth` 的 nonce 计数器、owner 记录 —— 这些在工装镜像里**一件都不存在**，也不需要为它们让路。





**这条直接解掉的一个死结**：RTC 一直没敢动，理由是「备份域里住着 `iap_auth` 的 nonce 计数器，初始化会清掉它」。**在工装镜像里那个计数器不存在**，所以备份域可以随便初始化。剩下的唯一障碍是时钟源（见 [HARDWARE-FACTS.md](../hardware/HARDWARE-FACTS.md) 的 RTC 一节）。





⚠️ **这条只管工装镜像。** bootloader 那边这些机制一个都不能少。





## 34 · 面板改了参数就存回方案文件；限值仍然不可编辑





用户 2026-09-09：**「面板改了，就直接存方案里。」**





**决定**：面板上改动一个**参数**（周期、字节数、波特率这类「怎么测」），点保存就写回当前选中的方案文件。**限值（`checks`）仍然完全不可编辑**，[第 30 条](#30--限值靠换一份方案文件调不给可编辑的输入框)那条一个字不改。





**参数和限值的界线，这是这条决定的全部要点**：





| | 是什么 | 面板能不能改 |


|---|---|---|


| **参数** `params` | 怎么测 —— `period=2000`、`bytes=1048576`、`baud=115200` | ✅ 能改，能存回 |


| **限值** `checks` | 什么算合格 —— `miss count_zero`、`vdda range 2400..2600` | ❌ 不能。只能整份换具名方案 |





**为什么这条界线站得住**：放宽一个限值是**让不合格的板子通过**，那是产线指南明令禁止的事；改一个参数只是换一种测法，测出来的读数照旧要过同一套限值。rs485 今天就是这个形状 —— 周期从 500 ms 改成 2000 ms 是参数，而判据反倒更严了（多加了 `overrun count_zero`）。





**实现上必须守住的**：写回时**只序列化 `params`**，`checks` 原样保留。一次把整个步骤对象写回去就会把限值也带上，那是这条决定最容易被破坏的方式。





**留痕**：写回时往面板日志写一行（哪个方案、哪一步、哪个参数、从什么改成什么）。方案文件本身在 git 里，所以谁改了什么由 git 记着 —— 这也是为什么参数存回是安全的而限值不是：`limit_version` 不会因为改了个周期而变，报告里那份追溯链没有被动过。





⚠️ **产线跑的不是 git 里那份**，是打包在 exe 旁边的 `Output/windows/plans/` 副本。所以「留痕靠 git」只对工程师的机器成立 —— 这正是限值必须由**服务端**而不是页面守住的原因：`$TOOL/internal/ptpanel/plan.go` 的 `keepLimits` 在每次保存时把 `checks` 和 `limit_version` 从磁盘那份原样取回，页面发什么都不算数。





## 35 · DIN 导轨扩展口不测





用户 2026-09-10 的原话：**「DIN 导轨扩展口先不测，没法测。」**





**决定**：`JunctionLink J4`（20 pin，Samtec ERF8）整个不进工装的测试范围。**不是「暂缓」，是这一版不做** —— 上面那 20 个脚（SPI2 / I2C2 / UART4 / SPI6 / JTAG 四线 / Debug Trigger）一个都不测。





**为什么这条不是遗憾**：那个口上没有任何东西是**只能**从它验的。SPI2、I2C2、SPI6 在这块板上没有别的用户；JTAG 四线是 ST-Link 每次烧录都在用的，烧得进去就说明它通。真正独占那个口的只有 `PH13/PH14` 的 TTL 串口，而它和工装自己的命令通道共用 UART4（[HARDWARE-FACTS.md](../hardware/HARDWARE-FACTS.md) 的「UART4 与 USART3」一节），测它就要放弃控制台。





**什么情况下重开**：有了能插到 J4 上的工装板，能同时给那 20 个脚加激励、读回应 —— 那时才谈得上测它。在那之前写任何代码都是写完没法验。





## 36 · RTC 时钟源从 LSI 换成 LSE





用户 2026-09-10：**「同意改 lse。」**





**决定**：三边（bootloader / 工装镜像 / 客户 app）的 RTC 全部改用 **LSE**（外部 32.768 kHz 晶振）。改 `.ioc` 重新生成，不做只给工装打补丁的分叉。





**硬件本来就齐**：晶振 **XTAL2**（ABS07 系列，Bridge 板顶层，在贴片坐标里）、备份电池 **BAT1 = VL1220/1HF**。固件从来没用上 —— Core/Src/rtc.c:74（`$BOOT/Core/Src/rtc.c`） 写死 `RCC_RTCCLKSOURCE_LSI`，`.ioc` 里 LSE 压根没打开（`RCC.RTCFreq_Value=32000` 是 LSI 的值）。





**为什么不能凑合用 LSI**：LSI 是片内 RC，±5% 量级并随温度漂，**一天最坏能差一个多小时**。产线报告里的时间戳、升级事件 journal 的时间、客户 app 的日历控制，三样都因此没有追溯价值。LSE 是石英，±20 ppm 量级，一天约 2 秒。





**为什么不只给工装改**：补丁会让 bootloader 和 app 继续用漂的钟，以后还要再改一遍，而且三份镜像的时间对不上。





⚠️ **LSI 不会被删**：独立看门狗（IWDG）只能由 LSI 驱动，那是硬件规定，和 RTC 无关。





⚠️ **这是 CubeMX 生成区**，重新生成后有两项必查 —— [CUBEMX-RULES.md](../build/CUBEMX-RULES.md)。另外 LSE 起振要几百 ms 到 2 s，比 LSI 慢得多，**启动路径上任何等 RTC 就绪的地方都要重新看一遍超时**。





## 37 · 测试方式统一成「单次 / 持续（选时长）」，`soak` 端口删掉





用户 2026-09-12：**「我既然可以持续测试多个端口，还有必要留 soak 么」**、**「不需要留，单项测试里面可以抽出来统一的测试方式按钮，单词，持续（选时间，1234 小时，一直）就这种。每个接口都可以控制，也可复选多个再选测试方式。」**





**决定，三件事**：





1. **测试方式是所有端口共用的一组选项**，不再是某个端口的特权：**单次** / **持续**，持续后面选时长 —— **1 / 2 / 3 / 4 小时 / 一直**。


2. **端口可以复选**，选好测试方式一起启动。


3. **`soak` 这个端口删掉**，连同 `plans/soak-2h.json`。





**为什么删得掉**：固件从来就能同时跑多个会话 —— `cmd_start` 没有任何互斥检查，`tick_sessions` 每轮把所有在跑的端口都 tick 一遍（porttool.c（`$BOOT/TestCase/porttool/porttool.c`））。`soak` 的「五项同跑」不是它独有的能力，只是把五个会话打包成了一个。打包这件事本身不值得占一个端口。





⚠️ **一并核实掉的两条误解**（2026-09-12 查证）：继电器的慢速翻转**不是** `soak` 独有 —— `relay` 会话自己的下限就是 1000 ms（porttool_relay.c（`$BOOT/TestCase/porttool/porttool_relay.c`） `RELAY_PERIOD_MIN_MS`），方案里写 `period=30000` 一样慢。VREFBUF 起不来就拒绝启动也**不是** `soak` 独有 —— `ain` / `aout` / `temp` 各自都有那道门。





**计时在 PC，判定也在 PC，板子只发数据。**





用户 2026-09-12 先说「由 pc 计时并控制板子启停，测试过程中，板子也要自检」，随即自己纠正：**「所有判别依据都在 pc，板子就是纯纯的发送数据。」后一句作准** —— 所以本文件**第 22 条一字不改**，固件继续只吐原始值。





**最终形状，六件**：





1. **时长由 PC 计**，到点 PC 发 `pt.stop`。会话不加 `minutes=`，固件没有自停定时器。


2. **板子不自检、不判任何东西。**


3. 判定全在上位机；判出故障 → PC 停测试 + 提示 + 点故障灯。


4. **故障灯只表达「有故障」**，不表达是哪个端口。PE2 只一盏、无回读，用闪烁次数编号架子旁边的人不会数。


5. **一个端口报错，全部停。**


6. `soak` 删掉。





⚠️ **唯一的例外是安全联锁**：计时在 PC 手里，所以 PC 崩了板子会一直驱动 24 V 到有人按复位。**那不是判测试结果，是判「还有没有人在听」** —— 和第 22 条不冲突。





### 看门狗：`pt.hold <ms>`，续期式





用户 2026-09-12 在两个方案里选了这个（**「选 b」**）。





**PC 周期性地说「我还在，再给我 N 毫秒」，板子到期就关输出。板子不判任何东西，它只是个定时器。**





| 命令 | 板子做什么 | 回 |


|---|---|---|


| `pt.hold 6000` | `hold_until = now + 6000`，武装 | `OK hold=6000` |


| `pt.hold 0` | 解除武装 | `OK hold=off` |


| `pt.hold` | 查询 | `OK hold=6000 left=4213` |





到期时：`stop_all()` 放掉所有输出，**并自己点亮故障灯** —— 正常情况下灯由 PC 点（上面第 3 件），而看门狗触发时 PC 已经不在了，不自己点就没人点。再推一行 `!hold ... expired=1`。





**参数**：`hold = 6000 ms`，**PC 每 2000 ms 续一次**（容忍连丢两次）。这两个数字没有物理依据，是工程取舍：太短会因为浏览器切标签 / 串口忙一下就误停，太长则真断了要等那么久 24 V 才关。





**只有「持续」模式武装**：单次测试几秒钟就完，工程师手工敲命令调试也不该被自停打扰。





**为什么不复用现成的 `miss` 计数**（那是被否决的 A 方案）：五个 `loop=link` 端口的应答走被测链路，`miss` 涨说明链路断了而不是 PC 没了，语义是错的；面板上「自动回环应答」是可关的勾，关了会误触发；而且那把一个**测量计数器**兼职成了**安全联锁**，以后改 echo 语义的人会不知不觉改掉 24 V 的兜底。





⚠️ **三件实现时要当心的**：





1. **`hold_check()` 必须能被测试手工推进时间** —— 照 `tick_sessions(now_ms)` 的样子把 `now_ms` 当参数传，别在函数里自己取 `HAL_GetTick()`。H4 的 harness 里那个时钟不走，这个坑踩过两次。


2. **`pt.hold` 走的是同一条 UART4 命令通道**，串口被帧挤爆时续期命令会排队。所以 hold 时长必须大于最坏排队延迟 —— **这和带宽限制是同一件事，带宽守住了这里就安全，守不住两头一起坏。**


3. **`pt.caps` 头部加一行 `hold=1`**，上位机零硬编码是这套的原则，老固件配新面板靠它知道该不该发，而不是发了被 `ERR unknown command` 打回来。





### 剩下三条一并定了的





| | |


|---|---|


| 串口带宽 | 面板在勾选时算「端口数 × 频率」，超了拦住或自动放慢。115200 = 11520 B/s，一个 ctrl 端口每帧走三趟（推帧 ~90 B + PC 回 `pt.echo` ~16 B + 板子回 `OK` ~20 B）；五个 × 200 ms ≈ 2.8 KB/s 好，八个 × 50 ms ≈ 17.6 KB/s 超。而 `printf` 阻塞，一堵超循环就慢，**所有端口的采样周期一起失准且无人报错** |


| 4 小时的日志 | **边跑边写文件**。面板现在只有浏览器里的 `MAX_LOG = 20000` 行，五个 ctrl 端口 × 200 ms × 3 行/帧 ≈ 75 行/秒，**只存得下最后约 4.5 分钟** —— 跑 4 小时烧机，第一小时发生了什么静静地没了 |


| 故障灯的实现 | 新命令让 PC 点灯（不能用现有的 `pt.run led.blink` —— 它是阻塞的一次性六闪，不是持续告警） |





---





## 38 · 上位机是监工：板子只干活并把执行日志全部发回来





用户 2026-09-13 原话：**「我现在要做的就是上位机软件就是监工，板子把所有执行的任务 log 全返回来，上位机来决定跑多久跑什么跑的怎么样。按照这个思想，没用的东西都去掉，该补的必要的赶紧补。」**





**决定，四句**：





1. **跑多久** —— 上位机计时。板子没有自停定时器（第 37 条已定）。


2. **跑什么** —— 上位机下发。板子不自己决定测序，也不自己循环。


3. **跑得怎么样** —— 上位机判（第 22 条已定）。


4. **日志全部发回来** —— 板子执行的每一件事都要从控制口出去，**不允许存在“只有人坐在串口前面看才知道结果”的测试**。





**这条相对第 22 条新在哪里**：第 22 条只管「谁下结论」。本条把范围扩到「谁控制过程」 —— 一个不返回、只打散文、要人看串口的测试，在「上位机是监工」这个思想下没有位置。






**什么情况下重开**：某一项测试被证实**做成会话就测不准**（不是“跑得慢”，是“结论会错”）。那一项才单独例外，并且要在这里记一条。





---





## 39 · 产测方案的边界：没有陪测板、没有外接仪器，板子自证 + log 上来





用户 2026-09-13 原话：**「没有陪测板，一切以板子整体测试为方案」**、**「目前没有外部仪器，只能是 log 上来，上位机读取来看，并且，硬件自己跳线自己看示波器和万用表」**。





这条界定的是《生产测试指导文件 V0.1》（`Hardware/OPENPLC生产测试指导文件_V0.1_20260904.docx`）里那一批「测试软件读 PC 外接电压电流模块 / 信号发生仪 / 陪测板」的需求。





**决定**：





1. **不写任何外接仪器驱动。** 没有型号、没有实物，写了也验不了。


2. **能由板子自证的，全部做成会话 / `pt.run`，数据从控制口上来，上位机判。**


3. **板子根本没有通路的，不做自动化** —— 列成人工项，硬件工程师自己跳线、自己用示波器和万用表看。


4. **扩展口仍然不测**（第 35 条）—— 本条把理由从「没有能插 J4 的工装板」扩到「根本没有陪测板」。





⚠️ **已核实的硬件事实：板子测不了任何电压和电流。** `Hardware/` 的 overview 与 GPIO 分配表里，24V / 3V3 / 5V0 / 5V_EXT **没有任何分压采样**，VNQ5160K-E 的电流 sense **没有回到 MCU**。板上只有两路温度（PA0 短路保护 / PA3 Fets）和对外的 AIN/AOUT。所以文档里**待机功耗、最高功耗、3.3V/5V/5V_EXT 电压、高边输出电流、继电器触点电流**这几项，**无论软件怎么写都拿不到数** —— 它们是人工项，不是没写完的功能。





**什么情况下重开**：真的有了陪测板或可编程仪器，并且型号与通信协议已冻结。那时候才谈得上写驱动。



---

## 41 · 测试的每一项都是上位机的配置项，分类 + 说明 + 用法 + 互斥



用户 2026-09-13 原话：**「你把所有测试的内容在上位机中都做成配置项。注意各项之间的互斥关系。并且给配置项分类。每项有说明和用法。」**



**决定，四件**：



1. **产线工程师不改代码就能配出一个工站的完整测试序列。** 凡是能变的东西都是配置项，没有藏在代码里的选择。

2. **配置项要分类**，不是一大片平铺的输入框。

3. **每一项旁边有这一项是干什么的、怎么填** —— 人不用查文档。

4. **互斥关系要在界面上当场说出来**，不是试了才报错。两项打架、一项要另一项才成立、某个值超了硬件上限 —— 勾上的那一刻就要看得到。



**总表在** [../test/PROD-CONFIG-ITEMS.md](../production/PROD-CONFIG-ITEMS.md)：九个分类、每项的说明与用法、十四条互斥关系及其出处。



⚠️ **这条不推翻第 30 条和第 34 条** —— **限值仍然不可在面板上编辑**，靠换一份方案文件调。本条管的是「跑什么、怎么跑」，不是「多少算过」。



**什么情况下重开**：某一项被证实做成配置项会让人配出一个**会损坏硬件**的组合，而且拦不住。那一项才回到写死。


---

## 42 · 总线类的产线判据是帧数与错误计数，不是速率

用户 2026-09-13 同意。起因是《生产测试指导文件 V0.1》3.6 / 3.10 把 **Ethernet / USB / CAN / RS485 / KNX 五个口统一写成**「向固定设备上传/下载文件，读取上传/下载速率并判定」。

**决定，分两类**：

| 类 | 口 | 判据 |
|---|---|---|
| **有吞吐概念** | `eth`、`usb` | 保留**速率判据** + 收发字节数。门限待定（文档写 TBD），由 `PTG-02` 回答 |
| **总线** | `can`、`rs485`、`knx` | **发出多少帧 / 收回多少帧 / 丢帧率 / 总线错误计数器归零** —— CAN 的 `tec`/`rec`/`lec`、RS485 的 `overrun`、各口的 `miss` |

**为什么不能照文档写**：总线上没有「文件」这个东西；而且 **KNX TP1 是固定 9600 bps**，速率既不会变、也不反映链路好坏，拿它当判据等于判了一个恒量。五个口的会话帧里本来就有这些字段，不用新增能力。

**连带要改文档的还有一处**：「USB 通信测试：向固定 USB 设备上传/下载文件」—— 板子是 **Device**，PC 才是主机（文档自己在「关键观察」栏也写了 `USB为Device`）。

⚠️ **「异常可恢复」还没定。** 文档 3.10 判定栏要求「全部配置接口通过且异常可恢复」并记「误码/恢复时间」，**现在没有任何一项测「拔掉再插回来能不能恢复」**（拔线是人工动作）。进了 `ISS-D1`（../work/ISSUES.md）。

**什么情况下重开**：产线真的配了能跑文件传输的 Golden 节点，而且客户要求报告里出现速率这一行。

---

## 43 · DO→DI 那根八芯线是一种接法，不是用例；面板不固化它

用户 2026-09-14 纠正。原话：**「DO - DI 连接，是我使用的方法，我可以这么接，也可以不这么接。不是一个必然的做法，不应该作为测试用例固化在上位机中。你应该做的就是 di 开启以后，等待输入，do 负责输出。」**

**决定**：

| 端口 | 面板负责什么 | 面板不负责什么 |
|---|---|---|
| `din` | 起会话，**等输入**，把八路电平报上来 | 不假设那 24 V 是从哪来的 |
| `dout` | 起会话，**按配置输出** | 不假设输出接到了哪里 |

**删掉的**：面板里那张「DO → DI 对照」卡（逐路驱动 + 矩阵对角判据），以及 `din`/`dout` 卡上所有指向它的话。

**为什么**：那根线是这张工作台上的一种**夹具选择**。产线可能用工装板把八路拉高，可能用别的夹具，也可能就是人拿根线点。把其中一种接法做成面板上的用例，等于替产线定了夹具 —— 而面板**分不出对面接的是什么**，和 [第 39 条](#39--产测方案的边界没有陪测板没有外接仪器板子自证--log-上来)「板子测不了它自己身上没有的通路」是同一件事。

**判据不跟着改**：`din` 仍然是 `v eq 0xFF`（八路全高），因为那是**这一轮要求的激励状态**，不是对夹具的要求。八路怎么被拉高，是工位的事。

⚠️ **「高边输出真出没出 24 V」还是没人证明。** 板上没有回读，`dout` 的判据只能是回显。[第 39 条](#39--产测方案的边界没有陪测板没有外接仪器板子自证--log-上来) 已经说了这一条，这里不另开。怎么证由工位的夹具回答，不由面板回答。

**什么情况下重开**：产线定了唯一的夹具，并且要求面板照着那一种接法判。那时它是**那个工位的方案文件**，仍然不是面板里写死的卡片。


---

## 44 · 判据和限值由硬件工程师写进方案，我方只报电平

用户 2026-09-14 定。原话：**「你只反馈电平，或者收集电平，判断标准由硬件工程师写方案，他们会把如何判断是否合格写到方案里面」**

**决定**：[第 22 条](#22--判定一律在上位机固件只吐原始值) 再往前一步 —— 判定不仅不在固件，**也不在上位机的代码里**。

| | 住哪 | 谁写 |
|---|---|---|
| 原始值（引脚电平、计数器、毫伏、原始码） | 帧里的字段 | 我方 |
| 算子（`eq` `ne` `lt` …） | `$TOOL/internal/ptcheck` | 我方 |
| **多少算合格、哪个电平算故障** | **方案文件 `$TOOL/TestCase/plans/*.json` 的 `checks`** | **硬件工程师** |

**直接后果**：

- **`ef1=` / `ef2=` 的极性不再是我方的未决项。** 代码侧已经齐了 —— 帧里有字段，`ptcheck` 写 `ef1 eq 0` 还是 `ef1 eq 1` 都支持。写哪一边是方案的事。
- **所有 TBD 限值同理**（功耗、模拟精度、速率门限……）。它们不是“我方欠的代码”，是方案文件里等着被填的格子。

⚠️ **我方仍然欠一件事**：方案文件里得有那几行的**位置和一个 TBD 占位**，否则硬件工程师不知道往哪写。空着等于没交付。

**什么情况下重开**：某条判据必须在帧到达之前就生效（例如安全联锁）。那类不是判据，是动作，不走这条。

---

## 45 · 校准值住在 bootloader 状态扇区的最后 8 个槽，reclaim 负责搬它

用户 2026-09-14 定。原话：**「就是最后一个扇区，因为 boot 会在擦之前先把补偿值读出来，另行处理。」**

| | |
|---|---|
| 放哪 | `IAP_STATE_SECTOR_ADDR`（`0x081E0000`）扇区的最后 8 个槽，`IAP_JOURNAL_SLOT_COUNT` 4096 → 4088 |
| 谁写 | **上位机，经 JLINK**（用户同日定：「上位机软件来写校准值」）。**固件不加写入命令** |
| 谁保它活着 | `journal_reclaim()` 擦整扇区之前读进 RAM，擦完写回 |
| app 上限 | 不变 1792K；擦除守卫不动 |

**为什么不另切一个扇区**：那要 app 少 128K，还要动唯一那道挡在 app 上传和 bootloader 状态之间的擦除守卫。让 reclaim 多搬 256 字节便宜得多。

**为什么是 8 个槽不是一个固定位置**：flash word 写过一次不能再写，重新校准必须能覆盖 —— 所以这一小块也是 append，最新的非空槽生效。

⚠️ **上位机不需要懂 journal 的记录格式**，只需要地址 + 结构体 + 「找第一个空槽写」。这是刻意的：懂了就会变成又一个跨仓镜像项。

⚠️ **这一条的代码 AI 不写。** 用户 2026-09-14：**「校准码代码不用写，你就写个 todo 就行，后面我会处理。」** TODO 在 `../work/ISSUES.md` 的 `ISS-C1`。

**什么情况下重开**：校准值大到 8 个槽装不下。那时先加槽数，仍然不另切扇区。

---

## 46 · 继电器只证明通断，不参与任何持续测试

用户 2026-09-15 定。原话：**「主要 relay 的寿命，响几下通了没问题了，就跳过，不要一直测。」**

**决定**：继电器的测试是**一次性的最小验证** —— 吸合、读回状态、停。不逐模式扫，不进持续测试。

**为什么这条不是洁癖**：HF41F 的机械寿命是 **3 万次**。持续测试按 1 秒周期算：

| 跑多久 | 动作次数 | 占额定寿命 |
|---|---|---|
| 1 小时 | 3,600 | 12 % |
| **4 小时（老化上限）** | **14,400** | **48 %** |
| 两轮 4 小时 | 28,800 | **96 %，基本报废** |

**一轮老化吃掉将近一半寿命，两轮就没了。** 而老化那一项（[第 39 条](#39--产测方案的边界没有陪测板没有外接仪器板子自证--log-上来)）的用意是让**别的**东西长时间跑，继电器跟着一起响没有任何好处。

**已经落到代码里的**：

| 在哪 | 做了什么 |
|---|---|
| `$TOOL/TestCase/host/porttool_panel/naive.py` 的 `WEARS_OUT` | 全端口扫描时 relay 只跑方案里那一个模式，不逐模式扫 |
| 面板 | 勾了 relay 又选「持续」时**当场拦住**，并把次数和额定寿命算给人看 |

⚠️ **面板原来只在说明里写了一句「触点有寿命」。** 一句警告挡不住一次误操作，而这个误操作是不可逆的。

**什么情况下重开**：产线真的要求做继电器寿命试验。那时它是**一个专门的工位**，有自己的计数和报废判据，不是搭在老化里顺带跑。

---

## 47 · AO 的会话卡和多点测量卡合并，方案里只做回显

用户 2026-09-15 定。两张卡用的是同一条 `pt.start aout`、同一个会话，差别只有两项：测几个点、读不读表 —— 那是两个配置项，不是两张卡。

**合并后一张卡，四个配置项**：通道、测哪几个点、**读数来自（不读 / 人工读表）**、合格判据（只在读表时生效）。

| 读数来自 | 判据 | 能无人值守 |
|---|---|---|
| **不读** | 方案给的回显判据（`ch1[0] eq 1000`） | ✅ |
| **人工读表** | 表读数 vs 期望，±% 满量程 + 直线拟合 | ❌ 每点要停下来等人 |

**方案文件里只有「不读表」那一档** —— 产线序列无人值守，一个会停下来等人的步骤会把整站挂住。⚠️ **不需要写校验来拦**：`meter` 是页面自己的状态，不在 `params` 里、不下发给板子、也进不了方案文件 —— 方案压根儿表达不了这一档。**校准只能人工在面板上做。**

⚠️ 选了读表就只能测一路：表一次只能串进一路回路。

**什么情况下重开**：有了能通过接口读数的仪器。那时它是「读数来自」的第三个取值，不是第三张卡。

---

## 48 · 一个 `pt.run` 目标可以有多组参数，判据按参数挑

用户 2026-09-15 定：**必要的合并，且用配置项实现合并前各自的功能**。

**合并了两处**：

| 原来 | 现在 |
|---|---|
| `sd.integrity`（一轮）+ `sd.stress`（64 轮） | **`sd.integrity`，轮数是 `passes=` 参数** —— 固件里 stress 本来就是 integrity 套个循环（`SD/sd_test.h`） |
| `sdram.retention`（一个周期）+ `sdram` 会话（一轮接一轮） | **只留会话** —— 面板上「单次 / 持续」那个开关就是它 |

**判据跟着参数走**：`criteriaFor()` 收下这次用的参数，在方案里挑**参数对得上**的那一步。所以 `sd.integrity passes=1` 和 `passes=64` 在方案里是两步、两组判据，面板上是一个按钮加一个可改的轮数。参数在方案里找不到对应的那一步，就只给读数不给结论 —— 和会话那边「改了参数就不给结论」是同一条规矩。

⚠️ **合并不能丢诊断。** 一轮和多轮报的字段刻意不同：一轮报**两个 CRC**（哪些字节读回来不对，只有单轮说得出），多轮报**第一次出错在第几轮**（只有多轮有这回事）。两者都报 `mounted / identical / passes / passed / bytes`，所以判据写法不变。这一条是 H4 抓出来的 —— 第一版合并把 CRC 丢了。

**什么情况下重开**：某个目标的两种参数需要**不同的字段**才能判，而不只是不同的限值。那时它是两个目标。
