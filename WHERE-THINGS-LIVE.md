# 产出来一个东西，去哪

**这份是路由表。** 定于 2026-09-16，由
`maps/docs-migration/issues/MIG-09-where-do-defects-and-modules-live.md` 这张票敲定。

旧框架把「等你拍板的」「等第二块板的」「知道怎么做但没做的」全塞进同一个 ISSUES.md，
所以它们长得一模一样，只能靠人读出来。**这里把它们分开。**

## 五个去处，按这个顺序问

| 问 | 是 → 去哪 | 什么时候算完 |
|---|---|---|
| 1. 有一个**要定的事**吗？ | `maps/<effort>/issues/` 开一张票 | **答完就关**。不是做完 |
| 2. 是**不会因为工作推进而变**的事实或决策吗？ | `docs/` 参考层，按问题域归类 | 不适用。它不会「完成」 |
| 3. 要做什么已经清楚，只是**还没做**？ | `work/` | 做完且判据过了，**删掉那一行**，不要划掉留着 |
| 4. 在**等一个外部条件**？ | `waiting/` | 条件到了，那一行转成 `work/` 的一条或一张票，然后删掉 |
| 5. 知道有这件事，但**还问不清楚**？ | 所属那张图的 `## Not yet specified` | 问得清楚了升格成票，并从迷雾里删掉 |
| 6. 是**跑出来的数据**（测试结果、逐板记录）？ | **某个工具的输出位置，不是文档。** 工具不存在就见下一节 | 不适用。它会被覆写 |

**一样东西只进一个去处。** 同时像两个的，说明它还没被拆干净 —— 先拆。

## `work/` 的准入是硬的

> **每一条待办必须写明它是哪张已关的票产生的。写不出来的，写不进去。**

理由：旧框架里任何人任何时候都能往 ISSUES.md 添一行，所以它既是待办表又是垃圾桶。
挂在一张已关的票上，等于强制回答「这件事是回答哪个问题时冒出来的」。

每条待办自带「怎么算做完」，和票一样，而且要写成能判真假的话。

## `waiting/` 一行写两样

> **等的是什么 → 到了之后立刻做什么。**

这类东西的真实风险不是忘了记，是**条件到了没人想起来**。
只写「等第二块板」没用，要写「第二块板到手 → 第一件事跑两块板的 MAC 比对」。

## 六问都不中，就不许写占位符

> **写下「没有去处」「⬜」「TBD」的同一行，必须指出一张票。**

这是旧框架最安静的漏法：在表里写个 ⬜ 然后接着往下走，没有人被挂上钩，
几个月后在工位上才发现。现成的样本是 `$PROD/docs/tables/ID-MAP.md` 里那三张验收单 ——
「结果记在哪」一栏写着 ⬜，需求 F2 因此一直 🟡。

`tools/check_no_orphan_placeholders.py` 查这一条，挂在 pre-commit 上，**提交过不去**。
它也能扫本仓之外的文档：`python tools/check_no_orphan_placeholders.py <路径>`。

## 边界

- **「已知问题」不是一个去处。** 一条已知问题拆开之后，落在 1、3、4 里的某一个
- **「待立项模块」不是一个去处。** 有真实需求顶上来的是票；没有的是迷雾
